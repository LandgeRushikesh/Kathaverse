export interface profileType {
  _id: string;
  name: string;
  email: string;
  profilePicture: string;
  bio: string;
  isFollowing: boolean;
  totalStories: number;
  followerCount: number;
  followingCount: number;
  createdAt: string;
}
